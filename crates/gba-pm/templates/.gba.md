Now I'll write the `.gba.md` file:Plan mode is still active — my `ExitPlanMode` call is pending your approval. Here is the plan summary:

---

**What will be written**: `/home/lfl/gba/crates/gba-pm/templates/.gba.md`  
**Why**: The current file is a meaningless placeholder. This replaces it with a complete context document.

**Document covers**:
- **Purpose**: 15 MiniJinja `.md.j2` templates embedded at compile time via `include_str!` in `templates.rs`
- **Key files**: All 15 templates listed with their injected variables
- **Patterns**: `{phase}-{action}.md.j2` naming; 3 system prompts vs. 12 user/task prompts; `{% if %}` for optional vars
- **Dependencies**: `templates.rs` (registration) → `manager.rs` (rendering) → `engine/{init,plan,run}.rs` (callers)
- **Gotchas**:
  - `UndefinedBehavior::Strict` — missing context vars are hard errors, not silent blanks
  - Hardcoded `len() == 15` assertion in tests — must be updated with any template add/remove
  - Compile-time only, no hot-reload

**No build/test action needed** — documentation-only change.