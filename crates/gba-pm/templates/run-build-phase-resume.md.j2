Resume work on **phase {{ phase_number }}**: {{ phase_description }}.

## IMPORTANT: This is a RESUME — a previous attempt was interrupted.

**Previous status**: `{{ previous_status }}`

## Resume Instructions

1. Run `git status` and `git diff` to see what changes have already been made.
2. Do NOT redo completed work — if files were already created/modified correctly, keep them.
3. If the build is broken or tests fail, fix them first.
4. Complete the remaining work for this phase.

## Design Spec

{{ design_spec }}

## Implementation Plan

{{ impl_plan }}

## Verification Spec

{{ verification_spec }}

Review the current state of the working directory first, then complete this phase.
